---
title: "Leverte IA-tjenester"
subtitle: Statistikken på denne siden viser antall digitale IA-tjenester levert i selvbetjente løsninger.
title-block-banner: true
title-block-banner-color: white
date: now
date-format: "DD.MM.YYYY HH:mm:ss z"
published-title: "Oppdatert"
author: Team IA
execute:
    echo: false
    warning: false
    daemon: false
format:
    html:
        toc: true
        toc-title: Indeks
        page-layout: full
        embed-resources: true
        theme: minty
        grid:
          body-width: 1000px
jupyter: python3
---
Statistikken på denne siden viser antall digitale IA-tjenester.

En digital IA-tjeneste telles når en bruker har benyttet seg av innholdet i
tjenesten. Det er ikke tilstrekkelig at brukeren kun har besøkt forsiden.

Dataregistreringen startet mars 2021.

NB! Dette er ikke offisiell statistikk og må ikke deles utenfor NAV.

<div style="color: gray;">
Kildekoden finnes på [GitHub](https://github.com/navikt/ia-datafortelling).
</div>

```{python}
# Hindre caching-problematikk ved lokal kjøring
%load_ext autoreload
%autoreload 2

from data_fetching import query_data
from data_prepping import prep_data

from ds import (
    create_bar_plot,
    create_line_plot,
    create_bar_plot_with_button,
    create_cumulative_histogram,
    create_table,
    markdown_table_row,
)

raw_data = query_data(limit=1000)
data = prep_data(raw_data)
```

# Unike virksomheter
```{python}
from ds import aaa
unike_virksomheter_bulletlist(data)


```{python}
from ds import plot_asd
plot_asd(data)
```

## Kululativt historgram av unike virksomheter
```{python}
from ds import plot_antall_unike_virksomheter_kumulativt
plot_antall_unike_virksomheter_kumulativt(data)
```


# Leverte digitale IA-tjenester per tjeneste/applikasjon
```{python}
from ds import (
    plot_tjenester_per_applikasjon,
    antall_per_app_tabell_mnd,
    antall_per_app_tabell_dager,
)

plot_tjenester_per_applikasjon(data)
```

### Månedsvis, siste to år
```{python}
antall_per_app_tabell_mnd(data)
```

### Siste 30 dager
```{python}
antall_per_app_tabell_dager(data)
```

# Informasjons- vs. interaksjonstjeneste i forebyggingsplan
```{python}
from ds import informasjon_vs_interaksjon
informasjon_vs_interaksjon(data)
```

# Fordeling antall ansatte
```{python}
from ds import fordeling_antall_ansatte
fordeling_antall_ansatte(data)
```

# Tilbakevendende brukere
Grafene viser antall og prosentandel av virksomheter som har fått levert digital
IA-tjeneste i gjeldende kvartal, og som samtidig fikk levert digital IA-tjeneste
kvartalet etter. Dette gir en indikasjon på hvor mange av virksomhetene som jobber
systematisk med IA.

```{python}
from ds import plot_tilbakevendende_brukere
plot_tilbakevendende_brukere(data)
```
